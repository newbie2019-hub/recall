/** Placeholder. Phase 7 — build a filtered deck. */
export function FilteredDeckPage({ mode }: { mode: 'new' | 'edit' }) {
  return <main className="p-8 text-muted-foreground">Not built yet ({mode}).</main>
}
